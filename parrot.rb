# Create method `parrot` that outputs a given phrase and
# returns the phrase
def parrot(phrase)
  puts "Squawk!"
  return "Squawk!"
  if phrase == nil
    puts "Squawk!"
  else
    puts "#{phrase}"
end
